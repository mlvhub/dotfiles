# Any key used is added to the running agent on first use (passphrase prompted
# once via the askpass helper), so you only ever unlock it a single time.
Host *
    AddKeysToAgent yes

Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/hetzner_mlvhub_id_ed25519
    IdentitiesOnly yes
